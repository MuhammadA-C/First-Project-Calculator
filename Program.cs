using System;

namespace Calculator_Project
{
    class Program
    {

        //Note: Going to use decimal as the data type

        static void Main(string[] args)
        {
           
        }

        decimal Addition(decimal number1, decimal number2)
        {
            decimal result = number1 + number1;
            return result;
        }

        decimal Subtraction(decimal number1, decimal number2)
        {
            decimal result = number1 - number2;
            return result;
        }

        decimal Multiplicaiton(decimal number1, decimal number2)
        {
            decimal result = number1 * number2;
            return result;
        }

        decimal Division(decimal number1, decimal number2)
        {
            decimal result = number1 / number2;
            return result;
        }
    }
    
}

